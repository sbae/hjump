[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hjump"
version = "0.3.1"
description = "Open VS Code Remote-SSH on a Slurm compute node via ProxyJump."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "typer>=0.12",
  "rich>=13.7",
]

[project.urls]
Homepage = "https://github.com/sbae/hjump"
Repository = "https://github.com/sbae/hjump"
Issues = "https://github.com/sbae/hjump/issues"

[project.scripts]
hjump = "hpc_jump.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["hpc_jump"]
